require 'spec_helper'

describe AwsSqsFaker do
  it 'has a version number' do
    expect(AwsSqsFaker::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
